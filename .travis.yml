# M68K isn't directly supported, so just use C.
language: c

script:
  - cd "${TRAVIS_BUILD_DIR}/Build Scripts" && python build.py s3
  - cd "${TRAVIS_BUILD_DIR}/Build Scripts" && python build.py sk
  - cd "${TRAVIS_BUILD_DIR}/Build Scripts" && python build.py verify

notifications:
  irc: "ircs://irc.badnik.zone:6697/#repo"
